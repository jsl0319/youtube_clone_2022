extends base.pug
include mixins/video

block content
        h2 Welcome to Wetube
        if videos.length
           each video in videos
             +video(video)
        else 
            h3 Not Found content!